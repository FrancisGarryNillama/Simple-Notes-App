export const API_BASE_URL = 'http://localhost:8080/api';

export const API_ENDPOINTS = {
  folders: `${API_BASE_URL}/folders`,
  notes: `${API_BASE_URL}/notes`,
  chain: `${API_BASE_URL}/chain`,  // NEW
};