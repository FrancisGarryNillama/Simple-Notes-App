# === MySQL Database Configuration ===
spring.datasource.url=jdbc:mysql://localhost:3306/notes_app
spring.datasource.username=root
spring.datasource.password=N8than#1

# === JPA / Hibernate Configuration ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jackson.time-zone=UTC

# === Chain / Wallet Secrets ===
CHAIN_API_KEY=${CHAIN_API_KEY}
BLOCKFROST_KEY=${BLOCKFROST_KEY}
BLOCKFROST_PROJECT_ID=preprodHVdJRIo0pB9OOwYefBDGdJyfK0zoWeuv
BLOCKFROST_NETWORK=${BLOCKFROST_NETWORK:cardano-preview}
